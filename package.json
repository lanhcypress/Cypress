{
  "name": "cypress-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "npx cypress open", 
    "cy:run": "npx cypress run",
    "cy:run:chrome": "npx cypress run --browser chrome",
    "test:login": "npx cypress run --spec \"cypress/e2e/login.cy.js\"",
    "test:dev": "npx cypress run --config baseUrl=https://example.com",
    "test:staging": "npx cypress run --config baseUrl=https://example.com",
    "test:prod": "npx cypress run --config baseUrl=https://example.com"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "cypress": "^15.18.1"
  }
}
